import { ShowCaseList } from '@/components/base';

export const ShowCaseSection: React.FC = () => {
  return (
    <section className="section bg-mediumBg">
      <div className="container">
        <ShowCaseList />
      </div>
    </section>
  );
};
